use super::schema::RawSutureSet;
use super::suture::Suture;
use crate::error::Error;

/// Compile a single suture_set.
pub(super) fn compile_suture_set(set: RawSutureSet) -> Result<Suture, Error> {
    todo!("compile: compile_suture_set")
}
