{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.sutures.dev/v1.json",
  "title": "Sutures DSL V1",
  "version": "1.0.2",
  "description": "Declarative field mapping between JSON payloads and typed structures",
  "type": "object",
  "required": ["name", "suture_sets"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1
    },
    "id": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "minLength": 1
    },
    "suture_sets": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/SutureSet"
      }
    }
  },

  "$defs": {
    "SutureSet": {
      "description": "A named group of sutures targeting a single capture direction (request or response)",
      "type": "object",
      "required": ["name", "capture", "sutures"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": "string"
        },
        "version": {
          "type": "string",
          "minLength": 1
        },
        "capture": {
          "type": "string",
          "enum": ["request", "response"]
        },
        "sutures": {
          "type": "array"
        }
      },
      "if": {
        "required": ["capture"],
        "properties": { "capture": { "const": "request" } }
      },
      "then": {
        "properties": {
          "sutures": { "items": { "$ref": "#/$defs/request_suture" } }
        }
      },
      "else": {
        "properties": {
          "sutures": { "items": { "$ref": "#/$defs/response_suture" } }
        }
      }
    },

    "json_terminal": {
      "description": "A JSON Pointer-like path starting with / that navigates into a JSON payload",
      "type": "string",
      "pattern": "^/[A-Za-z0-9_$.\\[\\]:?/`^()\\-+\\\\*|]*$"
    },

    "struct_terminal": {
      "description": "A path starting with a letter that navigates into a typed structure",
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_$.\\[\\]:?/`^()\\-+\\\\*|]*$"
    },

    "request_suture": {
      "description": "Maps struct field keys to JSON path values, with optional _ for injecting constants",
      "type": "object",
      "propertyNames": {
        "oneOf": [{ "const": "_" }, { "$ref": "#/$defs/struct_terminal" }]
      },
      "additionalProperties": {
        "oneOf": [
          { "$ref": "#/$defs/json_terminal" },
          {
            "type": "array",
            "items": { "$ref": "#/$defs/json_terminal" },
            "minItems": 1
          },
          { "$ref": "#/$defs/request_suture" }
        ]
      },
      "properties": {
        "_": {
          "type": "array",
          "items": {
            "type": "object",
            "minProperties": 1,
            "maxProperties": 1,
            "propertyNames": { "$ref": "#/$defs/json_terminal" },
            "additionalProperties": {
              "oneOf": [
                { "type": "string" },
                { "type": "number" },
                { "type": "boolean" },
                { "type": "null" }
              ]
            }
          }
        }
      }
    },

    "response_suture": {
      "description": "Maps JSON path keys to struct field values, with optional _ for injecting constants",
      "type": "object",
      "propertyNames": {
        "oneOf": [{ "const": "_" }, { "$ref": "#/$defs/json_terminal" }]
      },
      "additionalProperties": {
        "oneOf": [
          { "$ref": "#/$defs/struct_terminal" },
          {
            "type": "array",
            "items": { "$ref": "#/$defs/struct_terminal" },
            "minItems": 1
          },
          { "$ref": "#/$defs/response_suture" }
        ]
      },
      "properties": {
        "_": {
          "type": "array",
          "items": {
            "type": "object",
            "minProperties": 1,
            "maxProperties": 1,
            "propertyNames": { "$ref": "#/$defs/struct_terminal" },
            "additionalProperties": {
              "oneOf": [
                { "type": "string" },
                { "type": "number" },
                { "type": "boolean" },
                { "type": "null" }
              ]
            }
          }
        }
      }
    }
  }
}
